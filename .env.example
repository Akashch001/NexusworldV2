# NEXUS WORLD — ENVIRONMENT VARIABLES

# Supabase (Client Safe)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase (Server Only - Do NOT prefix with VITE_)
# SUPABASE_SERVICE_ROLE_KEY= (Configure in dashboard, not needed locally yet)

# AI Providers (Server Only - Do NOT prefix with VITE_)
# OPENCODE_API_KEY= (Configure in Supabase Edge Function Secrets)
